# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/orm_dashboard

# Security
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration (development)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Optional Settings
SQL_DEBUG=false
ENVIRONMENT=development

# For Railway deployment, these are automatically set:
# DATABASE_URL (provided by Railway PostgreSQL service)
# PORT (provided by Railway)

# For Render deployment:
# DATABASE_URL (from Render PostgreSQL add-on)
# PORT (provided by Render)